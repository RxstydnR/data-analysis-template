# スクリプト

* データ分析、AI予測など検証する場合は、`.py` ファイルを生成する。
* Interactive Windowでの実行を前提に、`# %%` 表記でセルを適切に分割する。
* 上から一括実行してすべて動作するようにする（順序依存で壊れる書き方をしない）。

## 保存先

* 対象プロジェクトに `scripts/` ディレクトリがあれば、その配下に保存する
* `scripts/` が存在しないプロジェクトの場合は、保存先をユーザーに確認する。

## 重複チェック

* ファイルを新規作成する前に、保存先ディレクトリに同じ検証内容・似た名前のファイルが既に無いか確認する。
* 既存ファイルが見つかった場合、新規に作らず既存ファイルへの追記・編集で対応してよいかユーザーに確認する。

## ファイル名の命名規則

* 検証内容が一目でわかるファイル名にする。多少冗長でもよい。
* `-`、`_` は使わない。各単語の先頭を大文字にする（CamelCase）。
* 先頭に2桁の採番をつける。これは実質ファイル数を示す。
  * 例: `01_`, `11_`
* ファイル名は英語、ファイルの中身（コメント・説明）は日本語で書く。
  * 例: `01_GPT5ReasoningSpeedComparison.py`、`02_MultipleLLMConversation.py`、`03_StreamingStructuredOutput.py`

## セルの区分け

* `# %% [markdown]` セル: 内容はすべてMarkdown形式で書く。コード例を載せる場合はコードブロックを使う。
* `# %%` セル: コードのみを書く。説明はPythonのコメント（`#`）で書き、Markdown記法は使わない。

## 章構成

* 最初のセルは `# %% [markdown]` とし、`#` で章タイトルを書き、その下に何を検証するのかの概要を書く。
* 以降のセクション区切りは `##` または `###` を使う。
* `####` 以下の見出しレベルは使わない。

## 依存情報の記載

新規`.py`を作成する際にデータ分析やデータ加工などそれが依存する`.py`に関しては、新規ファイルの先頭に記述する。

## コードの書き方

* コードにはコメントや説明を適宜追加する。
* 新しいライブラリやその新機能を検証する場合、関数や引数の説明を丁寧に書く。
* 必要以上に関数化・クラス化して読みやすくする、といったことはしない（適切な場合は行ってよい）。
* printデバッグは適宜追加する。
* コードセルの末尾で変数の中身を確認したい場合は、`変数 = ...` で終わらせず、セル最後の行に変数名だけを書いてInteractive Window上で値を表示させる。

```python
# %%
a = 1
b = 2
c = a + b
c
```





# ローカルファイルの読み書き

ローカルファイルの読み込みや書き込みを行う際は以下に従う。

## ルール

* すべてのファイルパス操作には `pathlib.Path` を使用してください。
* 絶対ローカルパスを**ハードコードしないでください**。
* リポジトリのルートまたは設定されたディレクトリからの相対パスを優先してください。
* 出力データをデータディレクトリ（`data/`）に**書き込まないでください**。
* 出力を書き込む際は、親ディレクトリを明示的に作成してください: `path.parent.mkdir(parents=True, exist_ok=True)`。
* 分かりやすいファイル名を使用してください。
* 出力が時間依存の場合は、ファイル名やフォルダ名に日付 'yyyymmdd-HHMMSS' を含めてください: `output_20260801-153000.csv`。出力が時間に依存しない場合は、タイムスタンプなしの固定名を使用しますが、保存する前にファイルが既に存在しないか確認してください: `if output_path.exists(): raise FileExistsError(f"{output_path} already exists")`
* 明示的に要求されない限り、既存の出力を上書きしないでください。

### 例

```python
from pathlib import Path

DATA_DIR = Path("data")
OUTPUTS_DIR = Path("outputs")

# 入力データの読み込み
input_path = DATA_DIR / "raw" / "titanic" / "train.csv"

# 出力パスを構成して書き込み
output_path = OUTPUTS_DIR / "tables" / "summary_2024q1.csv"
output_path.parent.mkdir(parents=True, exist_ok=True)

if output_path.exists():
	raise FileExistsError(f"{output_path} already exists")

df.write_csv(output_path)
```





# 安全なデータハンドリング

## 厳格なルール

* 生データ（Raw data）、認証情報、APIキー、トークン、顧客レベルのレコードを**絶対にコミットしないでください**。
* 生データを直接変更、上書き、削除、再生成することは**絶対にしないでください**。
* `data/raw/` および `data/external/` は変更不可（読み取り専用）として扱ってください。
* 派生データは `data/interim/`、`data/processed/`、または `outputs/` に書き込んでください。
* 出力を書き込む前に、ターゲットパスが `data/raw/` や `data/external/` の下になっていないことを**確認してください**。

## 推奨ワークフロー

1. 入力データが Raw（生データ）、External（外部データ）、Interim（中間データ）、Processed（処理済みデータ）、Output（出力）のどれであるかを**特定する**。
2. Raw/External データを変更不可の入力として**読み込む**（ソースファイルは絶対に直接変更しない）。
3. 生成された成果物は別の出力パス（`data/interim/`、`data/processed/`、`outputs/`）に**書き込む**。
4. 操作の最後に、読み込んだファイルと書き込んだファイルを**要約・報告する**。

## ディレクトリの役割
_
| ディレクトリ | 役割 | 変更（書き込み）可否 |
| --- | --- | --- |
| `data/raw/` | オリジナルの一次データ | 不可 |
| `data/external/` | サードパーティの参照データ | 不可 |
| `data/interim/` | 中間変換データ | 可能 |
| `data/processed/` | 最終的にクレンジング・加工されたデータ | 可能 |
| `outputs/` | 図表、テーブル、レポート | 可能 |

## PII（個人識別情報）および顧客レベルのレコード

* コミットするファイルに個人識別情報（PII）や顧客レベルの個別レコードを含めないでください。
* 分析に顧客レベルのデータが必要な場合は `data/raw/`（.gitignore指定済み）に保持し、絶対にコミットしないでください。
* 集計済みまたは匿名化されたデータであれば `data/processed/` や `outputs/` への保存が可能です。
* 判断に迷う場合は、顧客レベルのデータをパスに書き込む前に確認を取ってください。





# データ可視化

## 使用ライブラリ

* チャートの描画には **matplotlib** を使用します。
* テーマ設定、カラーパレット、統計プロットのために matplotlib と併せて **seaborn** を使用します。

## グローバル設定（最初に実行すること）

図表を生成するノートブックやスクリプトの冒頭で、テーマを一度だけ設定します。`font_scale` によりすべてのテキスト要素が比例して拡大されるため、個別の `fontsize=` 引数は基本的に不要です。

```python
import matplotlib.pyplot as plt
import seaborn as sns
sns.set_theme(
	style="whitegrid",
	palette="muted",
	font_scale=1.2,
)
```

### 日本語テキストの扱い

テキスト（タイトル、ラベル、凡例、注釈など）に日本語が含まれる場合は、CJK対応のフォントを設定してください。**設定しない場合、文字が文字化け（豆腐 □□□）になります。**

推奨されるアプローチ（クロスプラットフォーム対応）:

```python
import japanize_matplotlib  # uv add japanize-matplotlib

```

代替案（インストール済みのCJKフォントを明示的に指定）:

```python
plt.rcParams["font.family"] = "Noto Sans CJK JP"  # または "IPAexGothic", "Hiragino Sans", "Yu Gothic"

```

## 図（Figure）の作成

* 命令型の `plt.figure(...)` / `plt.plot(...)` スタイルは**使用しないでください**。
* **常に**図（Figure）と軸（Axes）を明示的に作成し、`tight_layout()` よりも `constrained_layout=True` を優先して使用してください:

```python
fig, ax = plt.subplots(figsize=(10, 6), constrained_layout=True)
```

* **オブジェクト指向（OO）API** を使用してください: `ax.set_title()`, `ax.set_xlabel()`, `ax.plot()` など。
* `figsize=(10, 6)` を標準のデフォルトとします。コンテンツに応じて調整してください（横長の時系列 → `(12, 4)`、正方形の散布図 → `(6, 6)`、マルチパネル → 適宜拡大）。

## カラーマップ / カラーパレット

データの種類に応じて選択してください:

* **カテゴリカル（分類データ）**: `"muted"`, `"Set2"`, `"colorblind"` (seaborn パレット)。
* **シーケンシャル（連続データ）**: `"viridis"`, `"cividis"`, `"mako"` — 知覚的に均一なパレット。
* **ダイバージング（発散データ）**: `"coolwarm"`, `"RdBu"`, `"vlag"`。
* **使用禁止**: `"jet"`, `"rainbow"` — 知覚的に不均一であり、色覚多様性（色弱者）に配慮されていません。

## フォントサイズ

`sns.set_theme(font_scale=1.2)` でほとんどのケースに対応できます。要素ごとにオーバーライドするのは**必要な場合のみ**にしてください（例: 長いタイトルを小さくしたい場合や、特定ラベルを強調したい場合など）:

```python
ax.set_title("...", fontsize=18)
```

`font_scale` が設定されている場合、すべての呼び出しで `fontsize=` を繰り返さないでください（冗長になります）。

## 軸スケールのガイドライン

* **棒グラフ**: y軸は必ず 0 から始めてください (`ax.set_ylim(bottom=0)`)。途中から切れた棒グラフは誤解を招きます。
* **折れ線グラフ / 散布図**: y軸を**無理に0から始めないでください**。有意な変動が隠れてしまうことがあります。matplotlibの自動スケールに任せるか、データの範囲に基づいて上限・下限を設定してください。
* **対数スケール**: データが複数の桁数（オーダー）にまたがる場合は `ax.set_yscale("log")` を使用してください。

## 凡例（Legend）

* 描画される系列が 2 つ以上ある場合のみ `ax.legend()` を使用してください。
* 自動配置でデータと重なる場合は、明示的に設定してください: `ax.legend(loc="upper left")` または枠外に配置: `ax.legend(loc="center left", bbox_to_anchor=(1.0, 0.5))`。

## 保存

* 最終的な図は `outputs/figures/` 配下に保存してください。

```python
from analysis_project.paths import outputs_dir, ensure_parent_dir

# 出力パスを構成して保存
output_path = outputs_dir() / "figures" / "survival_by_class.png"
ensure_parent_dir(output_path)
fig.savefig(output_path, dpi=150)
```

* 分かりやすい snake_case のファイル名を使用してください（`fig1.png` ではなく `monthly_sales_2024h1.png` など）。
* 解像度（DPI）の目安:
* ノートブック / README / スライド用: `dpi=150`
* 論文・出版 / 印刷用: `dpi=300`


* メモリを解放するため、保存後は必ず `plt.close(fig)` を呼び出してください。

```python
fig.savefig("outputs/figures/monthly_sales.png", dpi=150, bbox_inches="tight")
plt.close(fig)
```

## グラフ品質チェックリスト

チャートを確定する前に以下を確認してください:

* [ ] **タイトル**: チャートが何を示しているか明確に記述されているか。
* [ ] **軸ラベル**: 該当する場合、単位が含まれているか（例: "売上 (万円)", "Latency (ms)"）。
* [ ] **フォントサイズ**: 可読性があるか（ベースラインとして `font_scale=1.2` に依存）。
* [ ] **カラーパレット**: 知覚的に均一かつ色覚多様性に配慮されているか（jet/rainbowを使用していないか）。
* [ ] **日付範囲**: 関連する場合、タイトル、サブタイトル、または注釈に明記されているか。
* [ ] **サンプル/フィルタ注記**: データがサブセット化されている場合、その旨が記載されているか（例: "n=1,234, 2024年1月〜6月"）。
* [ ] **棒グラフ**: y軸が 0 から始まっているか。その他のグラフタイプでは適切な範囲が使われているか。
* [ ] **凡例**: 複数系列がある場合に表示されており、データと重なっていないか。
* [ ] **日本語テキスト**: 正しくレンダリングされているか（japanize-matplotlib または CJKフォントが設定されているか）。
* [ ] **保存**: 分かりやすいファイル名で `outputs/figures/` に保存され、直後に `plt.close(fig)` が実行されているか。

## 実装例

```python
from pathlib import Path
import matplotlib.pyplot as plt
import seaborn as sns
import japanize_matplotlib  # noqa: F401  # 日本語フォント有効化

sns.set_theme(style="whitegrid", palette="muted", font_scale=1.2)

Path("outputs/figures").mkdir(parents=True, exist_ok=True)

fig, ax = plt.subplots(figsize=(10, 6), constrained_layout=True)

ax.bar(categories, values)
ax.set_title("月別売上推移 (2024年1月〜6月)")
ax.set_xlabel("月")
ax.set_ylabel("売上 (万円)")
ax.set_ylim(bottom=0)  # 棒グラフは0起点

fig.savefig("outputs/figures/monthly_sales_2024h1.png", dpi=150, bbox_inches="tight")
plt.close(fig)
```





# 分析レポート作成（Analysis Reporting）

## 言語

* 特に指定がない限り、レポートは**日本語**で記述してください。

## 構成

1. **結論** — 結論または最も重要な発見から始めてください。
2. **事実** — データから得られた客観的な事実を提示してください。
3. **仮定** — 分析中に置いた前提条件・仮定を明記してください。
4. **解釈** — 解釈やそこから得られる示唆を提示してください。
5. **制約・注意点** — 限界、注意点、生じ得るバイアスについて言及してください。

## 必要な文脈（コンテキスト）

該当する場合は以下を含めてください:

* **データ期間**: 分析対象となったデータの期間。
* **フィルタ条件**: データに適用されたフィルタリング条件。
* **サンプルサイズ**: レコード数または観察データの数。
* **指標定義**: 主要指標（KPI）の計算方法。

## 再現性に関する注記

すべてのレポートには以下を含めるか、参照できるようにしてください:

* **入力データパス**: 使用した入力データへのパス。
* **クエリ/スクリプトパス**: 結果を生成したクエリ、ノートブック、またはスクリプトへのパス。
* **出力パス**: 生成物（図表、テーブル、レポート）への出力パス。
* **実行コマンド**: 結果を生成するために使用したコマンド。

## トーン（文体）

* ステークホルダー向けの要約には**ビジネスに適した言葉遣い**を使用してください。
* レビュアー向けの**技術的詳細**も確認できるようにしてください（付録やリンクされたノートブック内に記載）。
* よりシンプルな表現で十分な場合は、専門用語（ジャゴン）を避けてください。
* データが示している「事実」と、そこから推論された「意見・解釈」を明確に区別してください。





# Python スタイルガイド

## 型ヒント (Type Hints)

* すべての関数の引数と戻り値の型に型ヒントを追加してください。
* 複雑な型には `typing` モジュールを使用してください（例: `List`, `Dict`, `Optional`）。

## ドックストリング (Docstrings)

* すべてのパブリックなモジュール、クラス、関数、メソッドに対して **Googleスタイル** のドックストリングを使用してください。
* ドックストリングには以下を記載する必要があります:
* 目的 / 概要
* Args（引数）
* Returns（戻り値）
* Raises（発生する例外・該当する場合）
* Examples（使用例・役立つ場合）
* 重要な前提条件

## コメント

* **インラインコメントおよび説明コメントは必ず日本語で書いてください。**
* コメントでは、自明でない意図、前提条件、またはビジネスロジックについて説明してください。

## コードスタイル

* 実用的な範囲で、小さく副作用のない関数（純粋関数）を優先してください。
* 条件のない裸の `except` よりも、明示的なエラーハンドリングを優先してください。

### クラス

* クラスは必要に応じて使用しますが、過度なクラスの使用（不必要なカプセル化や階層化）は避けてください。





# 仮想環境

## コード実行

* `uv run`（例: `uv run python <ファイルパス>`）を使う。

## ライブラリの追加

* `uv add xxxx` を利用する

## パッケージマネージャー: uv のみを使用

* 依存関係のインストール、同期、追加、削除、更新にはすべて `uv` を使用してください。
* pip、pip3、`python -m pip`、poetry、conda、pipenv、easy_install は**絶対に**使用しないでください。
* `requirements.txt` を手動で作成・編集することは**絶対に**しないでください。
* 依存関係を追加するときは `uv add <パッケージ名>` を使用してください。
* 開発専用の依存関係には `uv add --group dev <パッケージ名>` を使用してください。
* 依存関係を変更した後は、`pyproject.toml` と `uv.lock` の差分を確認してください。